# Raspberry Pi Pico SDK port

The Pico SDK port will provide I2C slave/controller and UART transport adapters for RP2040 and RP2350.

The first hardware target will reuse the general lessons from AETERNA's Pico I2C node implementation while replacing the fixed AETERNA state packet with the public PicoMesh v1 frame format.
