name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  build-and-test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - name: Configure
        run: cmake -S . -B build -DPICOMESH_BUILD_TESTS=ON -DPICOMESH_BUILD_EXAMPLES=ON
      - name: Build
        run: cmake --build build --config Release --parallel
      - name: Test
        run: ctest --test-dir build -C Release --output-on-failure
      - name: Install
        run: cmake --install build --config Release --prefix "${{ github.workspace }}/install"
      - name: Configure installed-package consumer
        run: cmake -S tests/cmake_consumer -B consumer-build -DPicoMesh_DIR="${{ github.workspace }}/install/lib/cmake/PicoMesh"
      - name: Build installed-package consumer
        run: cmake --build consumer-build --config Release --parallel

  warnings-as-errors:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Configure
        env:
          CXXFLAGS: -Werror
        run: cmake -S . -B build-strict -DPICOMESH_BUILD_TESTS=ON -DPICOMESH_BUILD_EXAMPLES=ON
      - name: Build
        run: cmake --build build-strict --config Release --parallel
      - name: Test
        run: ctest --test-dir build-strict -C Release --output-on-failure
